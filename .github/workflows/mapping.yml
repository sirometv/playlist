name: Mapping

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-channels:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Download playlists
      run: |
        echo "📥 Downloading playlists..."
        curl -s -L -o main_playlist.m3u "https://raw.githubusercontent.com/sirometv/playlist/main/sirometv.m3u"
        curl -s -L -o source_playlist.m3u "https://raw.githubusercontent.com/sirometv/playlist/main/Ayna.m3u"
        
        for file in main_playlist.m3u source_playlist.m3u; do
          if [ ! -f "$file" ] || [ ! -s "$file" ]; then
            echo "❌ ERROR: $file download failed!"
            exit 1
          fi
          LINES=$(wc -l < "$file")
          echo "✅ $file downloaded - $LINES lines"
        done
        
    - name: Update All Channel
      run: |
        echo "🔄 Updating All Channel..."
        
        python3 - << 'EOF'
        import re
        from datetime import datetime
        import pytz
        
        # চ্যানেল ম্যাপিং - মেইন চ্যানেলের নাম : [সোর্স চ্যানেলের exact keywords]
        CHANNEL_MAPPING = {
            "T Sports": ["T Sports HD", "A Sports", "PTV Sports"],
            "BTV National": ["BTV NATIONAL HD", "BTV World"],
            "BTV CTG": ["BTV CTG"],
            "Channel 9": ["Channel 9"],
            "Channel I": ["Channel I"],
            "Bangla Vision": ["Bangla Vision"],
            "News 24": ["News 24 BD"], 
            "Boishakhi TV": ["Boishakhi TV"],
            "Bijoy TV": ["Bijoy TV"],
            "ATN Bangla": ["ATN Bangla"],
            "ATN-Bangla News": ["ATN News"],
            "DBC News": ["DBC News"],
            "Independent TV": ["Independent TV"],
            "Bangla TV": ["Bangla TV"],
            "Desh TV": ["Desh TV"],
            "RTV": ["RTV"],
            "My TV": ["My TV"],
            # "Somoy TV": ["Somoy News TV", "Somoy TV"],
            # এখানে আপনি আরো চ্যানেল যোগ করতে পারবেন
            # "Channel Name in Main": ["exact_keyword1", "exact_keyword2", "exact_keyword3"]
        }
        
        def get_bangladesh_time():
            """বাংলাদেশের বর্তমান সময় রিটার্ন করে"""
            bangladesh_tz = pytz.timezone('Asia/Dhaka')
            now = datetime.now(bangladesh_tz)
            return now.strftime("%Y-%m-%d %I:%M:%S%p BD Time") + " 🔄"
        
        def find_channel_urls(source_file, keywords):
            """সোর্স প্লেলিস্ট থেকে একাধিক চ্যানেলের URL খুঁজে বের করে"""
            with open(source_file, 'r', encoding='utf-8') as f:
                content = f.read()
            
            urls = []
            
            # সোর্স প্লেলিস্টকে লাইনে ভাগ করুন
            lines = content.split('\n')
            i = 0
            
            while i < len(lines):
                line = lines[i]
                # EXTINF লাইন খুঁজুন
                if line.startswith('#EXTINF:'):
                    extinf_line = line
                    channel_name_part = extinf_line.split(',', 1)[1] if ',' in extinf_line else ""
                    
                    # পরের লাইন URL হতে পারে
                    if i + 1 < len(lines) and lines[i + 1].startswith('http'):
                        url_line = lines[i + 1]
                        
                        # প্রতিটি keyword এর জন্য চেক করুন
                        for keyword in keywords:
                            # Exact match চেক - keyword চ্যানেল নামের মধ্যে exact match হতে হবে
                            # Case insensitive match
                            pattern = r'\b' + re.escape(keyword) + r'\b'
                            if re.search(pattern, channel_name_part, re.IGNORECASE):
                                print(f"✅ Exact match found: '{keyword}' in '{channel_name_part}'")
                                urls.append((keyword, url_line.strip()))
                                break  # একবার ম্যাচ পেলে পরের keyword চেক করার দরকার নেই
                        i += 2  # EXTINF এবং URL লাইন স্কিপ করুন
                    else:
                        i += 1
                else:
                    i += 1
            
            # যদি কোনো URL না পাওয়া যায়, তাহলে backup method ব্যবহার করুন
            if not urls:
                for keyword in keywords:
                    # Backup regex method
                    pattern = rf'#EXTINF:-1[^,]*,[^,]*\b{re.escape(keyword)}\b[^\n]*\n(http[^\n]+)'
                    match = re.search(pattern, content, re.IGNORECASE)
                    if match:
                        url = match.group(1).strip()
                        print(f"✅ Backup method found: '{keyword}' -> {url[:80]}...")
                        urls.append((keyword, url))
            
            if urls:
                print(f"✅ Total URLs found: {len(urls)}")
            else:
                print(f"❌ No URLs found for keywords: {keywords}")
            
            return urls
        
        def process_channel_with_auto_updated(lines, start_idx, channel_name, new_urls, is_manual_run):
            """একটি চ্যানেল প্রসেস করে AUTO-UPDATED সেকশন যোগ/আপডেট করে"""
            # চ্যানেলের শেষ খুঁজে বের করুন (পরবর্তী EXTINF বা ফাইল শেষ)
            end_idx = start_idx
            for i in range(start_idx + 1, len(lines)):
                if lines[i].startswith('#EXTINF:'):
                    end_idx = i - 1
                    break
                if i == len(lines) - 1:
                    end_idx = i
                    break
            
            print(f"   Channel block: lines {start_idx+1} to {end_idx+1}")
            
            # নতুন লাইন তৈরি করি
            new_block_lines = []
            
            # EXTINF লাইন যোগ করি
            new_block_lines.append(lines[start_idx])
            
            # এখন AUTO-UPDATED সেকশন খুঁজি
            auto_updated_start = -1
            auto_updated_end = -1
            
            for i in range(start_idx + 1, end_idx + 1):
                if lines[i].startswith('#AUTO-UPDATED'):
                    auto_updated_start = i
                    # AUTO-UPDATED সেকশনের শেষ খুঁজি
                    for j in range(i + 1, end_idx + 1):
                        if not (lines[j].startswith('http://') or lines[j].startswith('https://')):
                            auto_updated_end = j - 1
                            break
                    if auto_updated_end == -1:
                        auto_updated_end = end_idx
                    break
            
            timestamp = get_bangladesh_time()
            refresh_emoji = "🔄" * len(new_urls) if new_urls else ""
            timestamp_with_emoji = f"{timestamp.split(' 🔄')[0]} {refresh_emoji}"
            
            # নতুন AUTO-UPDATED সেকশন তৈরি করি
            new_auto_section = f"#AUTO-UPDATED - {channel_name} - {timestamp_with_emoji}\n"
            for keyword, url in new_urls:
                new_auto_section += f"{url}\n"
            
            if auto_updated_start != -1:
                print(f"   Found existing AUTO-UPDATED at line {auto_updated_start+1}")
                # AUTO-UPDATED সেকশন replace করি
                # প্রথমে EXTINF লাইনের পর থেকে auto_updated_start এর আগ পর্যন্ত সব লাইন যোগ করি
                for i in range(start_idx + 1, auto_updated_start):
                    new_block_lines.append(lines[i])
                
                # নতুন AUTO-UPDATED সেকশন যোগ করি
                new_block_lines.extend(new_auto_section.strip().split('\n'))
                
                # auto_updated_end এর পর থেকে end_idx পর্যন্ত সব লাইন যোগ করি
                for i in range(auto_updated_end + 1, end_idx + 1):
                    new_block_lines.append(lines[i])
                
                print(f"   Replaced AUTO-UPDATED section")
            else:
                print(f"   No AUTO-UPDATED section found, adding new one")
                # EXTINF লাইনের পরেই নতুন AUTO-UPDATED সেকশন যোগ করি
                # তারপর বাকি সব লাইন যোগ করি
                new_block_lines.extend(new_auto_section.strip().split('\n'))
                
                # EXTINF লাইনের পরের সব লাইন যোগ করি
                for i in range(start_idx + 1, end_idx + 1):
                    new_block_lines.append(lines[i])
                
                print(f"   Added new AUTO-UPDATED section")
            
            return new_block_lines, end_idx
        
        # মেইন প্লেলিস্ট লোড করুন
        with open('main_playlist.m3u', 'r', encoding='utf-8') as f:
            main_content = f.read()
        
        # Check if manual run or scheduled
        import os
        is_manual_run = os.environ.get('GITHUB_EVENT_NAME') == 'workflow_dispatch'
        
        print(f"📊 Run type: {'Manual' if is_manual_run else 'Scheduled'}")
        print(f"📊 Total channels in mapping: {len(CHANNEL_MAPPING)}")
        
        # লাইন বাই লাইন প্রসেস করি
        lines = main_content.split('\n')
        new_lines = []
        i = 0
        channels_updated = 0
        
        while i < len(lines):
            line = lines[i]
            
            # যদি EXTINF লাইন হয়
            if line.startswith('#EXTINF:'):
                # কোন চ্যানেলের জন্য চেক করছি
                current_channel = None
                for channel_name in CHANNEL_MAPPING.keys():
                    if channel_name in line:
                        current_channel = channel_name
                        break
                
                if current_channel:
                    print(f"\n{'='*50}")
                    print(f"🔍 Processing mapped channel: {current_channel} at line {i+1}")
                    
                    # এই চ্যানেলের জন্য URL খুঁজি
                    new_urls = find_channel_urls('source_playlist.m3u', CHANNEL_MAPPING[current_channel])
                    
                    if new_urls:
                        # চ্যানেল প্রসেস করি
                        channel_block_lines, new_i = process_channel_with_auto_updated(
                            lines, i, current_channel, new_urls, is_manual_run
                        )
                        
                        # নতুন লাইনগুলো যোগ করি
                        new_lines.extend(channel_block_lines)
                        
                        i = new_i
                        channels_updated += 1
                        print(f"✅ Updated {current_channel} with {len(new_urls)} new URLs")
                    else:
                        print(f"⚠️ No new URLs found for {current_channel}, keeping as is")
                        # নতুন URL না থাকলে পুরো চ্যানেল ব্লক অপরিবর্তিত রাখি
                        end_idx = i
                        for j in range(i + 1, len(lines)):
                            if lines[j].startswith('#EXTINF:'):
                                end_idx = j - 1
                                break
                            if j == len(lines) - 1:
                                end_idx = j
                                break
                        
                        for j in range(i, end_idx + 1):
                            new_lines.append(lines[j])
                        
                        i = end_idx
                else:
                    # ম্যাপ করা চ্যানেল না হলে অপরিবর্তিত রাখি
                    new_lines.append(line)
            else:
                # EXTINF লাইন না হলে অপরিবর্তিত রাখি
                new_lines.append(line)
            
            i += 1
        
        # নতুন কন্টেন্ট তৈরি করি
        updated_content = '\n'.join(new_lines)
        
        # আপডেট করা কন্টেন্ট সেভ করুন
        with open('sirometv_updated.m3u', 'w', encoding='utf-8') as f:
            f.write(updated_content)
        
        # ভেরিফিকেশন
        print(f"\n{'='*50}")
        print(f"🎉 Update Summary:")
        print(f"   Run type: {'Manual' if is_manual_run else 'Scheduled'}")
        print(f"   Total mapped channels: {len(CHANNEL_MAPPING)}")
        print(f"   Channels updated: {channels_updated}")
        
        # আসল এবং আপডেটেড কন্টেন্ট তুলনা
        original_lines = main_content.split('\n')
        updated_lines = updated_content.split('\n')
        
        print(f"\n📊 File Comparison:")
        print(f"   Original lines: {len(original_lines)}")
        print(f"   Updated lines:  {len(updated_lines)}")
        
        # T Sports সেকশন চেক
        print(f"\n🔍 Verifying T Sports section:")
        
        # আসল T Sports সেকশনে কতগুলো লিংক ছিল
        in_t_sports = False
        original_links = []
        for line in original_lines:
            if 'T Sports' in line and line.startswith('#EXTINF:'):
                in_t_sports = True
                continue
            if in_t_sports and line.startswith('#EXTINF:'):
                break
            if in_t_sports and (line.startswith('http://') or line.startswith('https://')):
                original_links.append(line)
        
        # আপডেটেড T Sports সেকশনে কতগুলো লিংক আছে
        in_t_sports = False
        updated_links = []
        for line in updated_lines:
            if 'T Sports' in line and line.startswith('#EXTINF:'):
                in_t_sports = True
                continue
            if in_t_sports and line.startswith('#EXTINF:'):
                break
            if in_t_sports and (line.startswith('http://') or line.startswith('https://')):
                updated_links.append(line)
        
        print(f"   Original T Sports links: {len(original_links)}")
        print(f"   Updated T Sports links:  {len(updated_links)}")
        
        if len(updated_links) >= len(original_links):
            print(f"   ✅ SUCCESS: All original links preserved!")
            
            # আসল লিংকগুলো আছে কিনা চেক
            missing_links = []
            for orig_link in original_links:
                if orig_link not in updated_links:
                    missing_links.append(orig_link)
            
            if missing_links:
                print(f"   ⚠️ WARNING: {len(missing_links)} original links missing!")
                for link in missing_links[:3]:
                    print(f"      - {link[:60]}...")
            else:
                print(f"   ✅ Perfect: All {len(original_links)} original links are preserved")
        else:
            print(f"   ❌ ERROR: {len(original_links) - len(updated_links)} links lost!")
        
        # T Sports সেকশন দেখান
        print(f"\n📋 T Sports section in updated file (first 25 lines after EXTINF):")
        in_section = False
        count = 0
        
        for line in updated_lines:
            if 'T Sports' in line and line.startswith('#EXTINF:'):
                in_section = True
                print(f"   [EXTINF]: {line}")
                count += 1
                continue
            
            if in_section:
                if line.startswith('#EXTINF:'):
                    break
                
                if count < 25:
                    if line.strip() == '':
                        print(f"   [EMPTY LINE]")
                    else:
                        print(f"   {line}")
                    count += 1
        
        EOF
        
    - name: Verify and replace playlist
      run: |
        echo "🔍 Verifying updated playlist..."
        
        if [ ! -f "sirometv_updated.m3u" ] || [ ! -s "sirometv_updated.m3u" ]; then
          echo "❌ ERROR: Updated playlist is missing or empty!"
          exit 1
        fi
        
        # T Sports সেকশনে আসল লিংক আছে কিনা চেক
        echo "🔍 Checking if original links are preserved in T Sports:"
        
        # আসল ফাইলে T Sports এর লিংকগুলো
        echo "   Original links in T Sports section:"
        awk '/T Sports/{p=1} p&&/^#EXTINF:/&&!/T Sports/{p=0} p&&/^http/{print "     - "$0}' main_playlist.m3u | head -10
        
        # আপডেটেড ফাইলে T Sports এর লিংকগুলো
        echo "   Updated links in T Sports section:"
        awk '/T Sports/{p=1} p&&/^#EXTINF:/&&!/T Sports/{p=0} p&&/^http/{print "     - "$0}' sirometv_updated.m3u | head -10
        
        # লিংক সংখ্যা তুলনা
        ORIG_TS_LINKS=$(awk '/T Sports/{p=1} p&&/^#EXTINF:/&&!/T Sports/{p=0} p&&/^http/{count++} END{print count}' main_playlist.m3u)
        UPD_TS_LINKS=$(awk '/T Sports/{p=1} p&&/^#EXTINF:/&&!/T Sports/{p=0} p&&/^http/{count++} END{print count}' sirometv_updated.m3u)
        
        echo ""
        echo "📊 T Sports link count comparison:"
        echo "   Original: $ORIG_TS_LINKS links"
        echo "   Updated:  $UPD_TS_LINKS links"
        
        if [ "$UPD_TS_LINKS" -ge "$ORIG_TS_LINKS" ]; then
          echo "   ✅ All original links preserved!"
        else
          echo "   ❌ ERROR: $((ORIG_TS_LINKS - UPD_TS_LINKS)) links lost!"
        fi
        
        # Show T Sports section structure
        echo ""
        echo "🔍 T Sports section structure:"
        grep -n -B1 -A 15 "T Sports" sirometv_updated.m3u | head -25
        
        # Count AUTO-UPDATED sections
        AUTO_COUNT=$(grep -c "#AUTO-UPDATED" sirometv_updated.m3u || true)
        echo ""
        echo "📊 AUTO-UPDATED sections: $AUTO_COUNT"
        
        mv sirometv_updated.m3u sirometv.m3u
        
    - name: Commit and push changes
      run: |
        echo "💾 Saving changes to repository..."
        
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add sirometv.m3u
        
        if git diff --staged --quiet; then
          echo "✅ No changes detected - playlist is up to date"
        else
          # বাংলাদেশের সময় নেওয়া (BD Time format)
          BANGLADESH_TIME=$(TZ='Asia/Dhaka' date +'%Y-%m-%d %I:%M:%S%p BD Time')
          
          # Check if manual or scheduled run
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            git commit -m "🔄 Manual Update: Preserved ALL Original Links - $BANGLADESH_TIME"
          else
            git commit -m "🔄 Scheduled Update: Preserved ALL Original Links - $BANGLADESH_TIME"
          fi
          git push
          echo "🎉 Successfully updated while preserving ALL original links!"
        fi
        
    - name: Cleanup
      run: |
        rm -f main_playlist.m3u source_playlist.m3u
        echo "✅ Cleanup completed!"
