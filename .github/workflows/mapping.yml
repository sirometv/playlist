name: Mapping

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-channels:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Download playlists
      run: |
        echo "📥 Downloading playlists..."
        curl -s -L -o main_playlist.m3u "https://raw.githubusercontent.com/sirometv/playlist/main/sirometv.m3u"
        curl -s -L -o source_playlist.m3u "https://raw.githubusercontent.com/sirometv/playlist/main/Ayna.m3u"
        
        for file in main_playlist.m3u source_playlist.m3u; do
          if [ ! -f "$file" ] || [ ! -s "$file" ]; then
            echo "❌ ERROR: $file download failed!"
            exit 1
          fi
          LINES=$(wc -l < "$file")
          echo "✅ $file downloaded - $LINES lines"
        done
        
    - name: Update All Channel
      run: |
        echo "🔄 Updating All Channel..."
        
        python3 - << 'EOF'
        import re
        from datetime import datetime
        import pytz
        
        # চ্যানেল ম্যাপিং - মেইন চ্যানেলের নাম : [সোর্স চ্যানেলের exact keywords]
        CHANNEL_MAPPING = {
            "T Sports": ["T Sports HD", "A Sports", "PTV Sports"],
            "BTV National": ["BTV NATIONAL HD", "BTV World"],
            "BTV CTG": ["BTV CTG"],
            "Channel 9": ["Channel 9"],
            "Channel I": ["Channel I"],
            "Bangla Vision": ["Bangla Vision"],
            "News 24": ["News 24 BD"], 
            "Boishakhi TV": ["Boishakhi TV"],
            "Bijoy TV": ["Bijoy TV"],
            "ATN Bangla": ["ATN Bangla"],
            "ATN-Bangla News": ["ATN News"],
            "DBC News": ["DBC News"],
            "Independent TV": ["Independent TV"],
            "Bangla TV": ["Bangla TV"],
            "Desh TV": ["Desh TV"],
            "RTV": ["RTV"],
            "My TV": ["My TV"],
            # "Somoy TV": ["Somoy News TV", "Somoy TV"],
            # এখানে আপনি আরো চ্যানেল যোগ করতে পারবেন
            # "Channel Name in Main": ["exact_keyword1", "exact_keyword2", "exact_keyword3"]
        }
        
        def get_bangladesh_time():
            """বাংলাদেশের বর্তমান সময় রিটার্ন করে"""
            bangladesh_tz = pytz.timezone('Asia/Dhaka')
            now = datetime.now(bangladesh_tz)
            return now.strftime("%Y-%m-%d %I:%M:%S%p BD Time") + " 🔄"
        
        def find_channel_urls(source_file, keywords):
            """সোর্স প্লেলিস্ট থেকে একাধিক চ্যানেলের URL খুঁজে বের করে"""
            with open(source_file, 'r', encoding='utf-8') as f:
                content = f.read()
            
            urls = []
            
            # সোর্স প্লেলিস্টকে লাইনে ভাগ করুন
            lines = content.split('\n')
            i = 0
            
            while i < len(lines):
                line = lines[i]
                # EXTINF লাইন খুঁজুন
                if line.startswith('#EXTINF:'):
                    extinf_line = line
                    channel_name_part = extinf_line.split(',', 1)[1] if ',' in extinf_line else ""
                    
                    # পরের লাইন URL হতে পারে
                    if i + 1 < len(lines) and lines[i + 1].startswith('http'):
                        url_line = lines[i + 1]
                        
                        # প্রতিটি keyword এর জন্য চেক করুন
                        for keyword in keywords:
                            # Exact match চেক - keyword চ্যানেল নামের মধ্যে exact match হতে হবে
                            # Case insensitive match
                            pattern = r'\b' + re.escape(keyword) + r'\b'
                            if re.search(pattern, channel_name_part, re.IGNORECASE):
                                print(f"✅ Exact match found: '{keyword}' in '{channel_name_part}'")
                                urls.append((keyword, url_line.strip()))
                                break  # একবার ম্যাচ পেলে পরের keyword চেক করার দরকার নেই
                        i += 2  # EXTINF এবং URL লাইন স্কিপ করুন
                    else:
                        i += 1
                else:
                    i += 1
            
            # যদি কোনো URL না পাওয়া যায়, তাহলে backup method ব্যবহার করুন
            if not urls:
                for keyword in keywords:
                    # Backup regex method
                    pattern = rf'#EXTINF:-1[^,]*,[^,]*\b{re.escape(keyword)}\b[^\n]*\n(http[^\n]+)'
                    match = re.search(pattern, content, re.IGNORECASE)
                    if match:
                        url = match.group(1).strip()
                        print(f"✅ Backup method found: '{keyword}' -> {url[:80]}...")
                        urls.append((keyword, url))
            
            if urls:
                print(f"✅ Total URLs found: {len(urls)}")
            else:
                print(f"❌ No URLs found for keywords: {keywords}")
            
            return urls
        
        def update_channel_urls(main_content, channel_name, channel_urls):
            """মেইন প্লেলিস্টে নির্দিষ্ট চ্যানেলে একাধিক URL আপডেট করে (AUTO-UPDATED সেকশনে)"""
            if not channel_urls:
                print(f"⚠️ No URLs to update for {channel_name}")
                return main_content
            
            timestamp = get_bangladesh_time()
            
            # Pattern: #EXTINF লাইন যেখানে channel_name আছে
            # Case insensitive search
            pattern_extinf = r'(#EXTINF:-1[^\n]*{}[^\n]*\n)'.format(re.escape(channel_name))
            
            match = re.search(pattern_extinf, main_content, re.IGNORECASE)
            if not match:
                print(f"❌ {channel_name} not found in main playlist")
                return main_content
            
            extinf_line = match.group(1)
            
            # URL সংখ্যা অনুযায়ী 🔄 ইমোজি তৈরি করুন
            refresh_emoji = "🔄" * len(channel_urls)
            
            # টাইমস্ট্যাম্পে ইমোজি যোগ করুন
            timestamp_with_emoji = f"{timestamp.split(' 🔄')[0]} {refresh_emoji}"
            
            # নতুন AUTO-UPDATED ব্লক তৈরি করুন
            auto_updated_block = f"#AUTO-UPDATED - {channel_name} - {timestamp_with_emoji}\n"
            
            # প্রতিটি URL যোগ করুন
            for keyword, url in channel_urls:
                auto_updated_block += f"{url}\n"
            
            print(f"📝 Updating {channel_name} with {len(channel_urls)} URLs")
            
            # কেস ১: যদি AUTO-UPDATED ব্লক ইতিমধ্যেই থাকে
            pattern_with_auto = r'(#EXTINF:-1[^\n]*{}[^\n]*\n)(#AUTO-UPDATED[^\n]*\n(?:http[^\n]*\n)*)'.format(re.escape(channel_name))
            
            match_with_auto = re.search(pattern_with_auto, main_content, re.IGNORECASE)
            
            if match_with_auto:
                # শুধুমাত্র AUTO-UPDATED ব্লক রিপ্লেস করুন
                def replacement(match):
                    return match.group(1) + auto_updated_block
                
                updated_content = re.sub(pattern_with_auto, replacement, main_content, flags=re.IGNORECASE)
                print(f"✅ Replaced AUTO-UPDATED block for {channel_name}")
                return updated_content
            else:
                # কেস ২: EXTINF এর পর সরাসরি URL আছে (আসল লিংক)
                # Pattern: EXTINF লাইন + URL লাইন
                pattern_extinf_with_url = r'(#EXTINF:-1[^\n]*{}[^\n]*\n)(http[^\n]*\n)'.format(re.escape(channel_name))
                
                match_url = re.search(pattern_extinf_with_url, main_content, re.IGNORECASE)
                
                if match_url:
                    # EXTINF লাইন + আসল URL এর পরে AUTO-UPDATED ব্লক যোগ করুন
                    def add_after_url(match):
                        return match.group(1) + match.group(2) + auto_updated_block
                    
                    updated_content = re.sub(pattern_extinf_with_url, add_after_url, main_content, flags=re.IGNORECASE)
                    print(f"✅ Added AUTO-UPDATED block after original URL for {channel_name}")
                    return updated_content
                else:
                    # কেস ৩: শুধু EXTINF লাইন আছে
                    # EXTINF লাইনের পরেই AUTO-UPDATED ব্লক যোগ করুন
                    pattern_extinf_only = r'(#EXTINF:-1[^\n]*{}[^\n]*\n)'.format(re.escape(channel_name))
                    
                    def add_auto_updated(match):
                        return match.group(1) + auto_updated_block
                    
                    updated_content = re.sub(pattern_extinf_only, add_auto_updated, main_content, flags=re.IGNORECASE)
                    print(f"✅ Added new AUTO-UPDATED block for {channel_name}")
                    return updated_content
        
        # মেইন প্লেলিস্ট লোড করুন
        with open('main_playlist.m3u', 'r', encoding='utf-8') as f:
            main_content = f.read()
        
        # Check if manual run or scheduled
        import os
        is_manual_run = os.environ.get('GITHUB_EVENT_NAME') == 'workflow_dispatch'
        
        updated_content = main_content
        total_urls_found = 0
        
        print(f"📊 Run type: {'Manual' if is_manual_run else 'Scheduled'}")
        
        # প্রতিটি চ্যানেলের জন্য আপডেট করুন
        for main_channel, search_keywords in CHANNEL_MAPPING.items():
            print(f"\n{'='*50}")
            print(f"🔍 Processing: {main_channel}")
            print(f"   Searching for exact keywords: {search_keywords}")
            
            # একাধিক URL খুঁজুন
            channel_urls = find_channel_urls('source_playlist.m3u', search_keywords)
            
            if channel_urls:
                print(f"   Found {len(channel_urls)} URLs: {[keyword for keyword, _ in channel_urls]}")
                updated_content = update_channel_urls(updated_content, main_channel, channel_urls)
                total_urls_found += len(channel_urls)
            else:
                print(f"⚠️ No URLs found for {main_channel}")
                
                # চেক করুন যে চ্যানেলটি মেইন প্লেলিস্টে আছে কিনা
                pattern_extinf = r'#EXTINF:-1[^\n]*{}[^\n]*'.format(re.escape(main_channel))
                if re.search(pattern_extinf, main_content, re.IGNORECASE):
                    print(f"   ℹ️ Channel '{main_channel}' exists in main playlist but no URLs found in source")
        
        # আপডেট করা কন্টেন্ট সেভ করুন
        with open('sirometv_updated.m3u', 'w', encoding='utf-8') as f:
            f.write(updated_content)
        
        print(f"\n{'='*50}")
        print(f"🎉 Update Summary:")
        print(f"   Run type: {'Manual' if is_manual_run else 'Scheduled'}")
        print(f"   Total channels processed: {len(CHANNEL_MAPPING)}")
        print(f"   Total URLs updated: {total_urls_found}")
        
        # মেইন প্লেলিস্টের কিছু অংশ দেখান (ডিবাগিং এর জন্য)
        print(f"\n📋 Sample of updated content (showing T Sports section):")
        t_sports_pattern = r'(#EXTINF:-1[^\n]*T Sports[^\n]*\n(?:.*\n){0,10})'
        match_sample = re.search(t_sports_pattern, updated_content, re.IGNORECASE)
        if match_sample:
            sample_lines = match_sample.group(1).split('\n')
            for i, line in enumerate(sample_lines[:15]):
                print(f"   {i+1}: {line}")
        
        EOF
        
    - name: Verify and replace playlist
      run: |
        echo "🔍 Verifying updated playlist..."
        
        if [ ! -f "sirometv_updated.m3u" ] || [ ! -s "sirometv_updated.m3u" ]; then
          echo "❌ ERROR: Updated playlist is missing or empty!"
          exit 1
        fi
        
        # Check for AUTO-UPDATED entries
        AUTO_COUNT=$(grep -c "#AUTO-UPDATED" sirometv_updated.m3u || true)
        echo "📊 AUTO-UPDATED entries found: $AUTO_COUNT"
        
        # Check for T Sports entries
        echo "🔍 Checking T Sports entries..."
        grep -n -A 5 "T Sports" sirometv_updated.m3u | head -20 || echo "No T Sports found"
        
        # Count total lines
        TOTAL_LINES=$(wc -l < sirometv_updated.m3u)
        echo "📊 Total lines in updated playlist: $TOTAL_LINES"
        
        mv sirometv_updated.m3u sirometv.m3u
        
    - name: Commit and push changes
      run: |
        echo "💾 Saving changes to repository..."
        
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add sirometv.m3u
        
        if git diff --staged --quiet; then
          echo "✅ No changes detected - playlist is up to date"
        else
          # বাংলাদেশের সময় নেওয়া (BD Time format)
          BANGLADESH_TIME=$(TZ='Asia/Dhaka' date +'%Y-%m-%d %I:%M:%S%p BD Time')
          
          # Check if manual or scheduled run
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            git commit -m "🔄 Manual Update: Multiple Channel Links - $BANGLADESH_TIME"
          else
            git commit -m "🔄 Scheduled Update: Multiple Channel Links - $BANGLADESH_TIME"
          fi
          git push
          echo "🎉 Successfully updated channels with multiple links!"
        fi
        
    - name: Cleanup
      run: |
        rm -f main_playlist.m3u source_playlist.m3u
        echo "✅ Cleanup completed!"
