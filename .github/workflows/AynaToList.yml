name: Ayna To Listing

on:
  schedule:
    - cron: '*/5 * * * *'  # প্রতি ৫ মিনিট পরপর
  workflow_dispatch:  # ম্যানুয়ালি রান করার অপশন

permissions:
  contents: write

jobs:
  sync-channels:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Setup Bangladesh timezone
      run: |
        echo "🌏 Setting up Bangladesh timezone..."
        sudo timedatectl set-timezone Asia/Dhaka
        echo "✅ Bangladesh timezone set: $(TZ='Asia/Dhaka' date)"
        
    - name: Download playlists
      run: |
        echo "📥 Downloading playlists..."
        
        # বাংলাদেশের সময় দেখান
        BD_TIME=$(TZ='Asia/Dhaka' date +'%Y-%m-%d %H:%M:%S')
        echo "🕐 Download started at: $BD_TIME (Bangladesh Time)"
        
        # মেইন প্লেলিস্ট এবং সোর্স প্লেলিস্ট ডাউনলোড
        curl -s -L -o main_playlist.m3u "https://raw.githubusercontent.com/sirometv/playlist/main/sirometv.m3u"
        curl -s -L -o source_playlist.m3u "https://raw.githubusercontent.com/sirometv/playlist/main/Ayna.m3u"
        
        # ডাউনলোড ভেরিফিকেশন
        for file in main_playlist.m3u source_playlist.m3u; do
          if [ ! -f "$file" ] || [ ! -s "$file" ]; then
            echo "❌ ERROR: $file download failed!"
            exit 1
          fi
          LINES=$(wc -l < "$file")
          CHANNELS=$(grep -c "#EXTINF:" "$file" 2>/dev/null || echo "0")
          echo "✅ $file downloaded - $LINES lines, $CHANNELS channels"
        done
        
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'
        
    - name: Update multiple channels
      run: |
        echo "🔄 Updating multiple channels..."
        
        # বাংলাদেশের সময় সংগ্রহ
        BD_TIME=$(TZ='Asia/Dhaka' date +'%Y-%m-%d %H:%M:%S Bangladesh Time')
        echo "🕐 Update process started at: $BD_TIME"
        
        python3 - << 'EOF'
        import re
        from datetime import datetime
        
        # বাংলাদেশের সময় সংগ্রহ
        current_time = datetime.now().strftime('%Y-%m-%d %H:%M:%S Bangladesh Time')
        
        # চ্যানেল ম্যাপিং - শুধু Exact Match হবে
        CHANNEL_MAPPING = {
            "BTV National": ["BTV NATIONAL HD", "BTV World"],
            "BTV CTG": ["BTV CTG"],
            "Somoy TV": ["Somoy TV"],
            "Jamuna TV": ["Jamuna TV"],
            "NTV BD": ["NTV"],
            "NTV-Europe": ["NTV Europe"],
            "Ekushey TV": ["Ekushey TV"],
            "Channel 24": ["Channel 24"],
            "Deen TV": ["Deen TV"],
            "SATV": ["SA TV"],
            "GTV": ["GTV"],
            "T Sports": ["T Sports HD"],
            "Channel 9": ["Channel 9"],
            "Channel I": ["Channel I"],
            "News 24": ["News 24 BD"],
            "Bangla Vision": ["Bangla Vision"],
            "ATN Bangla": ["ATN Bangla"],
            "ATN-Bangla News": ["ATN News"],
            "Asian TV": ["Asian TV"],
            "RTV": ["RTV"],
            "Ekhon TV": ["Ekhon TV"],
            "Duronto": ["Duronto TV"],
            "Deepto TV": ["Deepto TV"],
            "Mohona TV": ["Mohona TV"],
            "Ananda TV": ["Ananda TV"],
            "Bijoy TV": ["Bijoy TV"],
            "Boishakhi TV": ["Boishakhi TV"],
            "DBC News": ["DBC News"],
            "Independent TV": ["Independent TV"],
            "Bangla TV": ["Bangla TV"],
            "Desh TV": ["Desh TV"],
            "My TV": ["My TV"],
            "Ekattor TV": ["Ekattor TV"],
            "Peace TV Bangla": ["Peace TV Bangla HD"],
            "Rongeen TV": ["Rongeen TV"],
            "Gaan Bangla": ["Gaan Bangla"],
            "Star Jalsha": ["Star Jalsha"],
            "Star-Jalsha Movies": ["Star Jalsha Movies HD"],
            "Zee Bangla": ["Zee Bangla"],
            "Zee-Bangla Cinema": ["Zee Bangla Cinema"],
        }
        
        def find_exact_channel_urls(source_file, keywords):
            """সোর্স প্লেলিস্ট থেকে শুধু Exact Match চ্যানেলের URL খুঁজে বের করে"""
            try:
                with open(source_file, 'r', encoding='utf-8') as f:
                    content = f.read()
            except:
                with open(source_file, 'r', encoding='latin-1') as f:
                    content = f.read()
            
            found_urls = []
            
            # প্রতিটি keyword এর জন্য Exact Match করুন
            for keyword in keywords:
                try:
                    # Exact matching - শুধু সেই চ্যানেলগুলো নিন যেখানে keyword টি exact match
                    pattern = r'#EXTINF:[^\n]*\b{}\b[^\n]*\n(http[^\s]+)'.format(re.escape(keyword))
                    matches = re.findall(pattern, content, re.IGNORECASE)
                    
                    for url in matches:
                        clean_url = url.strip()
                        if clean_url and clean_url not in found_urls:
                            found_urls.append(clean_url)
                            print(f"✅ Found exact match URL for keyword '{keyword}': {clean_url}")
                            
                except Exception as e:
                    print(f"⚠ Error searching for '{keyword}': {e}")
                    continue
            
            return found_urls
        
        def update_channel_with_protection(main_content, channel_name, new_urls):
            """মেইন প্লেলিস্টে চ্যানেল আপডেট করে - Manual URLs সম্পূর্ণ সুরক্ষিত"""
            if not new_urls:
                print(f"⚠ No URLs found for {channel_name}, skipping update")
                return main_content
            
            # চ্যানেলের সম্পূর্ণ ব্লক খুঁজে বের করুন
            # আমরা শুধু EXTINF লাইন এবং পরের URLs নিব, কিন্তু MANUAL URLs চিহ্নিত করব
            pattern = r'(#EXTINF:[^\n]*{}[^\n]*\n)((?:#AUTO-UPDATED[^\n]*\n)?(?:http[^\n]*\n)*)'.format(re.escape(channel_name))
            match = re.search(pattern, main_content, re.IGNORECASE)
            
            if not match:
                print(f"❌ {channel_name} not found in main playlist")
                return main_content
            
            extinf_line = match.group(1)
            existing_block = match.group(2)
            
            # MANUAL URLs চিহ্নিত করার নতুন পদ্ধতি
            # আমরা MANUAL URLs কে #MANUAL-URL কমেন্ট দিয়ে চিহ্নিত করব
            lines = existing_block.split('\n')
            manual_urls = []
            auto_urls = []
            current_section = 'manual'  # default
            
            for line in lines:
                line = line.strip()
                if not line:
                    continue
                    
                if line.startswith('#AUTO-UPDATED'):
                    current_section = 'auto'
                    continue
                elif line.startswith('#MANUAL-URL'):
                    current_section = 'manual'
                    continue
                elif line.startswith('http'):
                    if current_section == 'manual':
                        # MANUAL URL কে চিহ্নিত করুন
                        if not line.startswith('#MANUAL-URL'):
                            manual_urls.append("#MANUAL-URL\\n" + line)
                        else:
                            manual_urls.append(line)
                    else:
                        auto_urls.append(line)
            
            # যদি MANUAL URLs না থাকে (প্রথমবার রান), তাহলে existing URLs গুলোকে MANUAL হিসেবে চিহ্নিত করুন
            if not manual_urls and existing_block.strip():
                for line in lines:
                    if line.startswith('http') and not line.startswith('#MANUAL-URL'):
                        manual_urls.append("#MANUAL-URL\\n" + line)
            
            print(f"📊 Found {len(manual_urls)} manual URLs for {channel_name}")
            
            # নতুন AUTO-UPDATED ব্লক তৈরি করুন
            new_auto_block = f"#AUTO-UPDATED - {current_time}\\n"
            for url in new_urls:
                new_auto_block += url + "\\n"
            
            # MANUAL URLs ব্লক
            manual_block = ""
            if manual_urls:
                manual_block = "\\n".join(manual_urls) + "\\n"
            
            # সম্পূর্ণ নতুন ব্লক তৈরি করুন
            new_complete_block = extinf_line + new_auto_block + manual_block
            
            # পুরাতন ব্লক রিপ্লেস করুন
            updated_content = main_content.replace(match.group(0), new_complete_block)
            
            print(f"✅ Successfully updated {channel_name}")
            print(f"   Auto URLs added: {len(new_urls)}")
            print(f"   Manual URLs preserved: {len(manual_urls)}")
            
            return updated_content
        
        try:
            # মেইন প্লেলিস্ট লোড করুন
            with open('main_playlist.m3u', 'r', encoding='utf-8') as f:
                main_content = f.read()
        except:
            with open('main_playlist.m3u', 'r', encoding='latin-1') as f:
                main_content = f.read()
        
        # Backup original content for safety
        original_content = main_content
        original_url_count = len(re.findall(r'^http[^\n]*\n', main_content, re.MULTILINE))
        
        # আপডেট প্রসেস শুরু
        updated_content = main_content
        update_count = 0
        successful_updates = []
        failed_updates = []
        
        # প্রতিটি চ্যানেলের জন্য আপডেট করুন
        for main_channel, search_keywords in CHANNEL_MAPPING.items():
            print(f"\\n🔍 Processing: {main_channel}")
            print(f"   Searching for exact matches: {search_keywords}")
            
            # শুধু Exact Match URLs নিন
            new_urls = find_exact_channel_urls('source_playlist.m3u', search_keywords)
            
            if new_urls:
                updated_content = update_channel_with_protection(updated_content, main_channel, new_urls)
                update_count += 1
                successful_updates.append(main_channel)
            else:
                print(f"⚠ No exact matches found for {main_channel}")
                failed_updates.append(main_channel)
        
        # Safety Check - Manual URLs সুরক্ষিত আছে কিনা
        updated_url_count = len(re.findall(r'^http[^\n]*\n', updated_content, re.MULTILINE))
        manual_url_count = len(re.findall(r'#MANUAL-URL', updated_content))
        
        print(f"\\n🔒 Safety Check Report:")
        print(f"   Original URLs: {original_url_count}")
        print(f"   Updated URLs: {updated_url_count}")
        print(f"   Manual URLs identified: {manual_url_count}")
        
        # যদি URLs কমে যায়, তাহলে original content restore করুন
        if original_url_count > updated_url_count:
            print("❌ CRITICAL: URLs count decreased! Manual URLs may be lost.")
            print("🔄 RESTORING ORIGINAL CONTENT FOR SAFETY")
            updated_content = original_content
            # শুধু header যোগ করুন
            header_comment = f"\\n# UPDATE FAILED: Manual URLs protection triggered at {current_time}\\n"
            with open('sirometv_updated.m3u', 'w', encoding='utf-8') as f:
                f.write(updated_content)
                f.write(header_comment)
            print("✅ Original content restored to protect manual URLs")
        else:
            # আপডেটেড টাইম সহ হেডার যোগ করুন
            header_comment = f"\\n# Auto-updated from Ayna.m3u\\n# Last Updated: {current_time}\\n# Total Channels Updated: {update_count}\\n# Manual URLs are protected with #MANUAL-URL tag\\n"
            
            # আপডেট করা কন্টেন্ট সেভ করুন
            with open('sirometv_updated.m3u', 'w', encoding='utf-8') as f:
                f.write(updated_content)
                f.write(header_comment)
            
            print("✅ Update completed successfully with manual URL protection")
        
        print(f"\\n🎉 Update Summary:")
        print(f"   Total channels processed: {len(CHANNEL_MAPPING)}")
        print(f"   Successfully updated: {update_count}")
        print(f"   Failed: {len(failed_updates)}")
        if successful_updates:
            print(f"   Updated channels: {', '.join(successful_updates)}")
        
        EOF
        
    - name: Verify and replace playlist
      run: |
        echo "🔍 Verifying updated playlist..."
        
        if [ ! -f "sirometv_updated.m3u" ] || [ ! -s "sirometv_updated.m3u" ]; then
          echo "❌ ERROR: Updated playlist is missing or empty!"
          exit 1
        fi
        
        # চ্যানেল কাউন্ট চেক করুন
        OLD_CHANNELS=$(grep -c "#EXTINF:" main_playlist.m3u 2>/dev/null || echo "0")
        NEW_CHANNELS=$(grep -c "#EXTINF:" sirometv_updated.m3u 2>/dev/null || echo "0")
        
        echo "📊 Channel Count:"
        echo "   Before: $OLD_CHANNELS channels"
        echo "   After: $NEW_CHANNELS channels"
        
        # URL কাউন্ট চেক করুন
        OLD_URLS=$(grep -c "^http" main_playlist.m3u 2>/dev/null || echo "0")
        NEW_URLS=$(grep -c "^http" sirometv_updated.m3u 2>/dev/null || echo "0")
        
        echo "📊 URL Count Verification:"
        echo "   Before: $OLD_URLS URLs")
        echo "   After: $NEW_URLS URLs")
        
        # MANUAL URLs চেক করুন
        MANUAL_COUNT=$(grep -c "#MANUAL-URL" sirometv_updated.m3u 2>/dev/null || echo "0")
        echo "🔒 Manual URLs protected: $MANUAL_COUNT")
        
        if [ "$OLD_URLS" -gt "$NEW_URLS" ]; then
          echo "❌ CRITICAL: Manual URLs were lost! Update was reverted."
        else
          echo "✅ URL safety check passed"
        fi
        
        # আপডেট করা চ্যানেলগুলো দেখান
        echo "🔍 Sample updated channels structure:"
        for channel in "Mohona TV" "Bangla TV" "SATV"; do
          echo "--- $channel ---"
          grep -A 10 "$channel" sirometv_updated.m3u | head -11 || echo "Not found"
          echo ""
        done
        
        mv sirometv_updated.m3u sirometv.m3u
        
    - name: Commit and push changes
      run: |
        echo "💾 Saving changes to repository..."
        
        # বাংলাদেশের সময় সংগ্রহ
        COMMIT_TIME=$(TZ='Asia/Dhaka' date +'%Y-%m-%d %H:%M:%S Bangladesh Time')
        
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add sirometv.m3u
        
        if git diff --staged --quiet; then
          echo "✅ No changes detected - playlist is up to date"
        else
          git commit -m "🔄 Auto Update Channels from Ayna - $COMMIT_TIME"
          git push
          echo "🎉 Successfully updated channels from Ayna playlist!"
          echo "🔒 Manual URLs are protected with #MANUAL-URL tags"
          echo "🛡️  Safety system will restore content if URLs are lost"
        fi
        
    - name: Cleanup
      run: |
        rm -f main_playlist.m3u source_playlist.m3u
        echo "✅ Cleanup completed!"
        echo "🏁 Ayna to Listing workflow finished successfully!"
