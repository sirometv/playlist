name: Ayna To T Sports & Other Channel 

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-channels:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Download playlists
      run: |
        echo "📥 Downloading playlists..."
        curl -s -L -o main_playlist.m3u "https://raw.githubusercontent.com/sirometv/playlist/main/sirometv.m3u"
        curl -s -L -o source_playlist.m3u "https://raw.githubusercontent.com/sirometv/playlist/main/Ayna.m3u"
        
        for file in main_playlist.m3u source_playlist.m3u; do
          if [ ! -f "$file" ] || [ ! -s "$file" ]; then
            echo "❌ ERROR: $file download failed!"
            exit 1
          fi
          LINES=$(wc -l < "$file")
          echo "✅ $file downloaded - $LINES lines"
        done
        
    - name: Update T Sports Channel
      run: |
        echo "🔄 Updating T Sports Channel..."
        
        python3 - << 'EOF'
        import re
        from datetime import datetime
        import pytz
        
        # চ্যানেল ম্যাপিং - আপনার প্রথম প্লেলিস্টের নাম : সেকেন্ড প্লেলিস্টে খুঁজবে এমন exact keywords
        CHANNEL_MAPPING = {
            "T Sports": ["T Sports HD", "A Sports", "PTV Sports"],
            "BTV National": ["BTV NATIONAL HD", "BTV World"],
            "BTV CTG": ["BTV CTG"],
            "Channel 9": ["Channel 9"],
            "Channel I": ["Channel I"],
            "Bangla Vision": ["Bangla Vision"],
            # "Maasranga TV": ["Maasranga TV"],  # uncomment করতে পারেন যদি প্রয়োজন হয়
            "News 24": ["News 24 BD"], 
            "Boishakhi TV": ["Boishakhi TV"],
            #  "Boishakhi Plus": ["Boishakhi Plus"],  # uncomment করতে পারেন যদি প্রয়োজন হয়
            "Bijoy TV": ["Bijoy TV"],
            "ATN Bangla": ["ATN Bangla"],
            "ATN-Bangla News": ["ATN News"],
            "DBC News": ["DBC News"],
            "Independent TV": ["Independent TV"],
            "Bangla TV": ["Bangla TV"],
            "Desh TV": ["Desh TV"],
            "RTV": ["RTV"],
            "My TV": ["My TV"],
            # "BTV NEWS": ["Somoy News TV", "Somoy TV"],  # uncomment করতে পারেন যদি প্রয়োজন হয়
            # "Somoy TV": ["Somoy News TV", "Somoy TV"],  # uncomment করতে পারেন যদি প্রয়োজন হয়
        }
        
        def get_bangladesh_time():
            """বাংলাদেশের বর্তমান সময় রিটার্ন করে"""
            bangladesh_tz = pytz.timezone('Asia/Dhaka')
            now = datetime.now(bangladesh_tz)
            return now.strftime("%Y-%m-%d %I:%M:%S%p BD Time")
        
        def find_channel_url(source_file, keywords):
            """সোর্স প্লেলিস্ট থেকে চ্যানেল URL খুঁজে বের করে (exact match)"""
            with open(source_file, 'r', encoding='utf-8') as f:
                content = f.read()
            
            # Exact match এর জন্য pattern তৈরি করুন
            for keyword in keywords:
                exact_pattern = r'#EXTINF:[^\n]*\b{}\b[^\n]*\n(http[^\n]+)'.format(re.escape(keyword))
                match = re.search(exact_pattern, content, re.IGNORECASE)
                
                if match:
                    url = match.group(1).strip()
                    print(f"✅ Found exact match for keyword '{keyword}': {url}")
                    return url
            
            print(f"❌ No exact match found for keywords: {keywords}")
            return None
        
        def update_channel_with_multiple_links(main_content, channel_name, search_keywords, is_manual_run=False):
            """একটি চ্যানেলের জন্য multiple links যোগ করে (manual links intact রাখবে)"""
            timestamp = get_bangladesh_time()
            
            # Pattern: #EXTINF লাইন যেখানে channel_name আছে
            pattern_extinf = r'(#EXTINF:-1[^\n]*{}[^\n]*\n)'.format(re.escape(channel_name))
            
            match = re.search(pattern_extinf, main_content, re.IGNORECASE)
            if not match:
                print(f"❌ {channel_name} not found in main playlist")
                return main_content, 0
            
            # সব keywords এর জন্য URLs সংগ্রহ করুন
            channel_urls = []
            for keyword in search_keywords:
                url = find_channel_url('source_playlist.m3u', [keyword])
                if url:
                    channel_urls.append(url)
            
            if not channel_urls:
                print(f"⚠️ No URLs found for any keywords of {channel_name}")
                return main_content, 0
            
            # Multiple URLs তৈরি করুন
            multiple_urls_content = f"#AUTO-UPDATED - Multiple Links - {timestamp}\n"
            for url in channel_urls:
                multiple_urls_content += f"{url}\n"
            
            # চ্যানেলের সম্পূর্ণ ব্লক খুঁজে বের করুন
            channel_block_pattern = r'(#EXTINF:-1[^\n]*{}[^\n]*\n)(.*?)(?=#EXTINF|\Z)'.format(re.escape(channel_name))
            channel_block_match = re.search(channel_block_pattern, main_content, re.IGNORECASE | re.DOTALL)
            
            if not channel_block_match:
                print(f"❌ Could not extract channel block for {channel_name}")
                return main_content, 0
            
            full_channel_block = channel_block_match.group(0)
            extinf_line = channel_block_match.group(1)
            existing_content = channel_block_match.group(2)
            
            if is_manual_run:
                # Manual run: নতুন AUTO-UPDATED section যোগ করবে বা replace করবে
                auto_updated_pattern = r'(#AUTO-UPDATED[^\n]*\n)(.*?)(?=#EXTINF|\Z|#AUTO-UPDATED)'
                auto_updated_match = re.search(auto_updated_pattern, existing_content, re.IGNORECASE | re.DOTALL)
                
                if auto_updated_match:
                    # AUTO-UPDATED section exists, replace only the AUTO-UPDATED part
                    updated_block = re.sub(auto_updated_pattern, multiple_urls_content, existing_content, flags=re.IGNORECASE | re.DOTALL)
                    print(f"✅ Manual run: Replaced AUTO-UPDATED section for {channel_name} with {len(channel_urls)} links")
                else:
                    # No AUTO-UPDATED section exists, add it at the beginning of the links
                    updated_block = multiple_urls_content + existing_content
                    print(f"✅ Manual run: Added new AUTO-UPDATED section for {channel_name} with {len(channel_urls)} links")
                
                # পুরো চ্যানেল ব্লক replace করুন
                updated_content = main_content.replace(full_channel_block, extinf_line + updated_block)
                return updated_content, len(channel_urls)
                
            else:
                # Scheduled run: শুধুমাত্র existing AUTO-UPDATED section-এর URLs replace করবে
                auto_updated_pattern = r'(#AUTO-UPDATED[^\n]*\n)(.*?)(?=#EXTINF|\Z|#AUTO-UPDATED|http)'
                auto_updated_match = re.search(auto_updated_pattern, existing_content, re.IGNORECASE | re.DOTALL)
                
                if auto_updated_match:
                    # AUTO-UPDATED section exists, replace only the URLs inside AUTO-UPDATED
                    current_auto_updated = auto_updated_match.group(0)
                    new_auto_updated = f"#AUTO-UPDATED - Multiple Links - {timestamp}\n"
                    for url in channel_urls:
                        new_auto_updated += f"{url}\n"
                    
                    updated_block = existing_content.replace(current_auto_updated, new_auto_updated)
                    print(f"✅ Scheduled run: Updated AUTO-UPDATED URLs for {channel_name} with {len(channel_urls)} links")
                else:
                    # No AUTO-UPDATED section exists, add it at the beginning
                    updated_block = multiple_urls_content + existing_content
                    print(f"✅ Scheduled run: Added AUTO-UPDATED section for {channel_name} with {len(channel_urls)} links")
                
                # পুরো চ্যানেল ব্লক replace করুন
                updated_content = main_content.replace(full_channel_block, extinf_line + updated_block)
                return updated_content, len(channel_urls)
        
        # মেইন প্লেলিস্ট লোড করুন
        with open('main_playlist.m3u', 'r', encoding='utf-8') as f:
            main_content = f.read()
        
        # Check if manual run or scheduled
        import os
        is_manual_run = os.environ.get('GITHUB_EVENT_NAME') == 'workflow_dispatch'
        
        updated_content = main_content
        total_links_added = 0
        
        # প্রতিটি চ্যানেলের জন্য আপডেট করুন
        for main_channel, search_keywords in CHANNEL_MAPPING.items():
            print(f"\n🔍 Processing: {main_channel}")
            print(f"   Searching for exact matches: {search_keywords}")
            print(f"   Run type: {'Manual' if is_manual_run else 'Scheduled'}")
            
            updated_content, links_added = update_channel_with_multiple_links(
                updated_content, main_channel, search_keywords, is_manual_run
            )
            total_links_added += links_added
        
        # আপডেট করা কন্টেন্ট সেভ করুন
        with open('sirometv_updated.m3u', 'w', encoding='utf-8') as f:
            f.write(updated_content)
        
        print(f"\n🎉 Update Summary:")
        print(f"   Run type: {'Manual' if is_manual_run else 'Scheduled'}")
        print(f"   Total main channels processed: {len(CHANNEL_MAPPING)}")
        print(f"   Total links added/updated: {total_links_added}")
        
        EOF
        
    - name: Verify and replace playlist
      run: |
        echo "🔍 Verifying updated playlist..."
        
        if [ ! -f "sirometv_updated.m3u" ] || [ ! -s "sirometv_updated.m3u" ]; then
          echo "❌ ERROR: Updated playlist is missing or empty!"
          exit 1
        fi
        
        mv sirometv_updated.m3u sirometv.m3u
        
        # আপডেট করা চ্যানেলগুলো দেখান
        echo "🔍 Updated channels (showing EXTINF and next 10 lines):"
        grep -A 10 -E "T Sports" sirometv.m3u || echo "No matching channels found"
        
    - name: Commit and push changes
      run: |
        echo "💾 Saving changes to repository..."
        
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add sirometv.m3u
        
        if git diff --staged --quiet; then
          echo "✅ No changes detected - playlist is up to date"
        else
          BANGLADESH_TIME=$(TZ='Asia/Dhaka' date +'%Y-%m-%d %I:%M:%S%p BD Time')
          
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            git commit -m "🔄 Manual Update: Multiple Links - $BANGLADESH_TIME"
          else
            git commit -m "🔄 Scheduled Update: Multiple Links - $BANGLADESH_TIME"
          fi
          git push
          echo "🎉 Successfully updated multiple links!"
        fi
        
    - name: Cleanup
      run: |
        rm -f main_playlist.m3u source_playlist.m3u
        echo "✅ Cleanup completed!"
