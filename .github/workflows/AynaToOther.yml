name: Ayna To T Sports & Other Channel 

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-channels:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Download playlists
      run: |
        echo "📥 Downloading playlists..."
        curl -s -L -o main_playlist.m3u "https://raw.githubusercontent.com/sirometv/playlist/main/sirometv.m3u"
        curl -s -L -o source_playlist.m3u "https://raw.githubusercontent.com/sirometv/playlist/main/Ayna.m3u"
        
        for file in main_playlist.m3u source_playlist.m3u; do
          if [ ! -f "$file" ] || [ ! -s "$file" ]; then
            echo "❌ ERROR: $file download failed!"
            exit 1
          fi
          LINES=$(wc -l < "$file")
          echo "✅ $file downloaded - $LINES lines"
        done
        
    - name: Update T Sports Channel
      run: |
        echo "🔄 Updating T Sports Channel..."
        
        python3 - << 'EOF'
        import re
        from datetime import datetime
        import pytz
        
        # চ্যানেল ম্যাপিং - আপনার প্রথম প্লেলিস্টের নাম : সেকেন্ড প্লেলিস্টে খুঁজবে এমন exact keywords
        CHANNEL_MAPPING = {
            "T Sports": ["T Sports HD", "A Sports", "PTV Sports"],
            "BTV National": ["BTV NATIONAL HD", "BTV World"],
            "BTV CTG": ["BTV CTG"],
            "Channel 9": ["Channel 9"],
            "Channel I": ["Channel I"],
            "Bangla Vision": ["Bangla Vision"],
            "News 24": ["News 24 BD"], 
            "Boishakhi TV": ["Boishakhi TV"],
            "Bijoy TV": ["Bijoy TV"],
            "ATN Bangla": ["ATN Bangla"],
            "ATN-Bangla News": ["ATN News"],
            "DBC News": ["DBC News"],
            "Independent TV": ["Independent TV"],
            "Bangla TV": ["Bangla TV"],
            "Desh TV": ["Desh TV"],
            "RTV": ["RTV"],
            "My TV": ["My TV"],
        }
        
        def get_bangladesh_time():
            """বাংলাদেশের বর্তমান সময় রিটার্ন করে"""
            bangladesh_tz = pytz.timezone('Asia/Dhaka')
            now = datetime.now(bangladesh_tz)
            return now.strftime("%Y-%m-%d %I:%M:%S%p BD Time") + " 🔄"
        
        def find_channel_url(source_file, keywords):
            """সোর্স প্লেলিস্ট থেকে চ্যানেল URL খুঁজে বের করে (exact match)"""
            with open(source_file, 'r', encoding='utf-8') as f:
                content = f.read()
            
            # Exact match এর জন্য pattern তৈরি করুন
            for keyword in keywords:
                exact_pattern = r'#EXTINF:[^\n]*\b{}\b[^\n]*\n(http[^\n]+)'.format(re.escape(keyword))
                match = re.search(exact_pattern, content, re.IGNORECASE)
                
                if match:
                    url = match.group(1).strip()
                    print(f"✅ Found exact match for keyword '{keyword}': {url}")
                    return url
            
            print(f"❌ No exact match found for keywords: {keywords}")
            return None
        
        def update_channel_url(main_content, channel_name, new_urls):
            """মেইন প্লেলিস্টে নির্দিষ্ট চ্যানেলের URL আপডেট করে (manual links intact রাখবে)"""
            timestamp = get_bangladesh_time()
            
            # Pattern: #EXTINF লাইন যেখানে channel_name আছে
            pattern_extinf = r'(#EXTINF:-1[^\n]*{}[^\n]*\n)'.format(re.escape(channel_name))
            
            match = re.search(pattern_extinf, main_content, re.IGNORECASE)
            if not match:
                print(f"❌ {channel_name} not found in main playlist")
                return main_content
            
            extinf_line = match.group(1)
            
            # Multiple URLs এর জন্য content তৈরি করুন
            auto_updated_urls = "".join([f"{url}\n" for url in new_urls])
            auto_updated_section = f"#AUTO-UPDATED - Multiple Links - {timestamp}\n{auto_updated_urls}"
            
            # চ্যানেলের সম্পূর্ণ ব্লক খুঁজে বের করুন
            channel_block_pattern = r'(#EXTINF:-1[^\n]*{}[^\n]*\n)(.*?)(?=#EXTINF|\Z)'.format(re.escape(channel_name))
            channel_block_match = re.search(channel_block_pattern, main_content, re.IGNORECASE | re.DOTALL)
            
            if not channel_block_match:
                print(f"❌ Could not extract channel block for {channel_name}")
                return main_content
            
            full_channel_block = channel_block_match.group(0)
            existing_content = channel_block_match.group(2)
            
            # AUTO-UPDATED section খুঁজে বের করার জন্য line by line check করুন
            lines = existing_content.split('\n')
            auto_updated_start = -1
            auto_updated_end = -1
            
            for i, line in enumerate(lines):
                if line.startswith('#AUTO-UPDATED - Multiple Links -'):
                    auto_updated_start = i
                    # AUTO-UPDATED section-এর শেষ খুঁজে বের করুন (পরবর্তী non-URL লাইন পর্যন্ত)
                    for j in range(i + 1, len(lines)):
                        if not lines[j].startswith('http'):
                            auto_updated_end = j
                            break
                    if auto_updated_end == -1:
                        auto_updated_end = len(lines)
                    break
            
            if auto_updated_start != -1:
                # AUTO-UPDATED section exists, শুধু এই section টি replace করুন
                print(f"🔍 Found AUTO-UPDATED section at lines {auto_updated_start}-{auto_updated_end}")
                
                # নতুন content তৈরি করুন
                new_lines = lines[:auto_updated_start]  # AUTO-UPDATED section-এর আগের অংশ
                new_lines.extend(auto_updated_section.split('\n'))  # নতুন AUTO-UPDATED section
                new_lines.extend(lines[auto_updated_end:])  # AUTO-UPDATED section-এর পরের অংশ (manual links)
                
                updated_existing_content = '\n'.join(new_lines)
                print(f"✅ Updated AUTO-UPDATED section for {channel_name} with {len(new_urls)} links")
            else:
                # No AUTO-UPDATED section exists, নতুন section যোগ করুন manual links-এর উপরে
                print(f"🔍 No AUTO-UPDATED section found, adding new one")
                updated_existing_content = auto_updated_section + existing_content
                print(f"✅ Added AUTO-UPDATED section for {channel_name} with {len(new_urls)} links")
            
            # পুরো চ্যানেল ব্লক replace করুন
            new_channel_block = extinf_line + updated_existing_content
            updated_content = main_content.replace(full_channel_block, new_channel_block)
            
            return updated_content
        
        # মেইন প্লেলিস্ট লোড করুন
        with open('main_playlist.m3u', 'r', encoding='utf-8') as f:
            main_content = f.read()
        
        # Check if manual run or scheduled
        import os
        is_manual_run = os.environ.get('GITHUB_EVENT_NAME') == 'workflow_dispatch'
        
        updated_content = main_content
        total_links_added = 0
        
        # প্রতিটি চ্যানেলের জন্য আপডেট করুন
        for main_channel, search_keywords in CHANNEL_MAPPING.items():
            print(f"\n🔍 Processing: {main_channel}")
            print(f"   Searching for exact matches: {search_keywords}")
            print(f"   Run type: {'Manual' if is_manual_run else 'Scheduled'}")
            
            # সব keywords এর জন্য URLs সংগ্রহ করুন
            channel_urls = []
            for keyword in search_keywords:
                url = find_channel_url('source_playlist.m3u', [keyword])
                if url:
                    channel_urls.append(url)
            
            if channel_urls:
                updated_content = update_channel_url(updated_content, main_channel, channel_urls)
                total_links_added += len(channel_urls)
            else:
                print(f"⚠️ Skipping {main_channel} - no URLs found")
        
        # আপডেট করা কন্টেন্ট সেভ করুন
        with open('sirometv_updated.m3u', 'w', encoding='utf-8') as f:
            f.write(updated_content)
        
        print(f"\n🎉 Update Summary:")
        print(f"   Run type: {'Manual' if is_manual_run else 'Scheduled'}")
        print(f"   Total channels processed: {len(CHANNEL_MAPPING)}")
        print(f"   Total links added/updated: {total_links_added}")
        
        EOF
        
    - name: Verify and replace playlist
      run: |
        echo "🔍 Verifying updated playlist..."
        
        if [ ! -f "sirometv_updated.m3u" ] || [ ! -s "sirometv_updated.m3u" ]; then
          echo "❌ ERROR: Updated playlist is missing or empty!"
          exit 1
        fi
        
        mv sirometv_updated.m3u sirometv.m3u
        
        # আপডেট করা চ্যানেলগুলো দেখান
        echo "🔍 Updated channels (showing EXTINF and next 15 lines):"
        grep -A 15 -E "T Sports" sirometv.m3u || echo "No matching channels found"
        
    - name: Commit and push changes
      run: |
        echo "💾 Saving changes to repository..."
        
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add sirometv.m3u
        
        if git diff --staged --quiet; then
          echo "✅ No changes detected - playlist is up to date"
        else
          BANGLADESH_TIME=$(TZ='Asia/Dhaka' date +'%Y-%m-%d %I:%M:%S%p BD Time')
          
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            git commit -m "🔄 Manual Update: Multiple Links - $BANGLADESH_TIME"
          else
            git commit -m "🔄 Scheduled Update: Multiple Links - $BANGLADESH_TIME"
          fi
          git push
          echo "🎉 Successfully updated multiple links!"
        fi
        
    - name: Cleanup
      run: |
        rm -f main_playlist.m3u source_playlist.m3u
        echo "✅ Cleanup completed!"
