name: Live Event Group Update

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-channels:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Download playlists
      run: |
        echo "📥 Downloading playlists..."
        curl -s -L -o main_playlist.m3u "https://raw.githubusercontent.com/sirometv/playlist/main/sirometvsports.m3u"
        curl -s -L -o source_playlist.m3u "https://raw.githubusercontent.com/abusaeeidx/Mrgify-BDIX-IPTV/main/playlist.m3u"
        
        for file in main_playlist.m3u source_playlist.m3u; do
          if [ ! -f "$file" ] || [ ! -s "$file" ]; then
            echo "❌ ERROR: $file download failed!"
            exit 1
          fi
          LINES=$(wc -l < "$file")
          echo "✅ $file downloaded - $LINES lines"
        done
        
    - name: Update Live Event Group
      run: |
        echo "🔄 Updating Live Event Group Channels..."
        
        python3 - << 'EOF'
        import re
        from datetime import datetime
        import pytz
        
        def get_bangladesh_time():
            """বাংলাদেশের বর্তমান সময় রিটার্ন করে"""
            bangladesh_tz = pytz.timezone('Asia/Dhaka')
            now = datetime.now(bangladesh_tz)
            return now.strftime("%Y-%m-%d %I:%M:%S%p BD Time")
        
        def extract_live_event_channels(source_file):
            """সোর্স প্লেলিস্ট থেকে Live Event গ্রুপের সব চ্যানেল extract করে"""
            channels = []
            
            with open(source_file, 'r', encoding='utf-8') as f:
                content = f.read()
            
            # Live Event গ্রুপের সব চ্যানেল খুঁজে বের করা
            pattern = r'(#EXTINF:-1[^\n]*group-title="[^"]*Live Event[^"]*"[^\n]*\n)(http[^\n]+)'
            
            matches = re.finditer(pattern, content, re.IGNORECASE)
            
            for match in matches:
                extinf_line = match.group(1).strip()
                url_line = match.group(2).strip()
                
                # চ্যানেল নাম extract করা
                channel_name_match = re.search(r',([^,\n]+)$', extinf_line)
                channel_name = channel_name_match.group(1).strip() if channel_name_match else "Live Match"
                
                # tvg-logo extract করা
                logo_match = re.search(r'tvg-logo="([^"]*)"', extinf_line)
                tvg_logo = logo_match.group(1) if logo_match else "https://i.postimg.cc/7P04hWZh/image.jpg"
                
                channels.append({
                    'extinf_line': extinf_line,
                    'url_line': url_line,
                    'channel_name': channel_name,
                    'tvg_logo': tvg_logo
                })
            
            print(f"✅ Found {len(channels)} Live Event channels in source playlist")
            return channels
        
        def replace_live_event_section(main_content, live_event_channels, is_manual_run=False):
            """মেইন প্লেলিস্টে Live Event সেকশন সম্পূর্ণ replace করে"""
            timestamp = get_bangladesh_time()
            
            # নতুন Live Event সেকশন তৈরি করা
            new_live_event_section = f"#AUTO-UPDATED - Live Event Group - {timestamp}\n"
            
            for channel in live_event_channels:
                # নতুন EXTINF লাইন তৈরি করা - group-title="Live Event" নিশ্চিত করা
                new_extinf_line = f'#EXTINF:-1 tvg-logo="{channel["tvg_logo"]}" group-title="Live Event", {channel["channel_name"]}\n'
                new_live_event_section += new_extinf_line
                new_live_event_section += channel["url_line"] + "\n\n"
            
            # মেইন প্লেলিস্টে existing Live Event সেকশন খুঁজে বের করা
            pattern = r'(#AUTO-UPDATED[^\n]*Live Event[^\n]*\n)(.*?)(?=\n#EXT|$)'
            match = re.search(pattern, main_content, re.IGNORECASE | re.DOTALL)
            
            if match:
                # যদি existing Live Event সেকশন থাকে, replace করবে
                updated_content = re.sub(pattern, new_live_event_section, main_content, flags=re.IGNORECASE | re.DOTALL)
                print("✅ Replaced existing Live Event section")
            else:
                # যদি existing Live Event সেকশন না থাকে, নতুন যোগ করবে
                # প্রথমে মেইন প্লেলিস্টের শেষে যোগ করার চেষ্টা করা
                if main_content.strip().endswith('#EXT'):
                    updated_content = main_content + "\n\n" + new_live_event_section
                else:
                    updated_content = main_content + "\n" + new_live_event_section
                print("✅ Added new Live Event section")
            
            return updated_content
        
        def find_live_event_marker(main_content):
            """মেইন প্লেলিস্টে Live Event মার্কার খুঁজে বের করা"""
            # "Live Match" বা similar চ্যানেল খোঁজা
            pattern = r'#EXTINF:-1[^\n]*Live[^\n]*\n(http[^\n]+)'
            match = re.search(pattern, main_content, re.IGNORECASE)
            return match
        
        # মেইন প্লেলিস্ট লোড করুন
        with open('main_playlist.m3u', 'r', encoding='utf-8') as f:
            main_content = f.read()
        
        # Check if manual run or scheduled
        import os
        is_manual_run = os.environ.get('GITHUB_EVENT_NAME') == 'workflow_dispatch'
        
        # সোর্স প্লেলিস্ট থেকে Live Event চ্যানেলগুলো extract করুন
        live_event_channels = extract_live_event_channels('source_playlist.m3u')
        
        if not live_event_channels:
            print("❌ No Live Event channels found in source playlist!")
            # সোর্স ফাইল কন্টেন্ট দেখানো (ডিবাগিং এর জন্য)
            with open('source_playlist.m3u', 'r', encoding='utf-8') as f:
                source_content = f.read()
                live_event_matches = re.findall(r'group-title="[^"]*Live Event[^"]*"', source_content, re.IGNORECASE)
                print(f"Debug: Found {len(live_event_matches)} group-title mentions with 'Live Event'")
            exit(1)
        
        # মেইন প্লেলিস্ট আপডেট করুন
        updated_content = replace_live_event_section(main_content, live_event_channels, is_manual_run)
        
        # আপডেট করা কন্টেন্ট সেভ করুন
        with open('sirometv_updated.m3u', 'w', encoding='utf-8') as f:
            f.write(updated_content)
        
        print(f"\n🎉 Update Summary:")
        print(f"   Run type: {'Manual' if is_manual_run else 'Scheduled'}")
        print(f"   Live Event channels added: {len(live_event_channels)}")
        print(f"   First few channels:")
        for i, channel in enumerate(live_event_channels[:3]):
            print(f"     {i+1}. {channel['channel_name']}")
        if len(live_event_channels) > 3:
            print(f"     ... and {len(live_event_channels) - 3} more channels")
        
        EOF
        
    - name: Verify and replace playlist
      run: |
        echo "🔍 Verifying updated playlist..."
        
        if [ ! -f "sirometv_updated.m3u" ] || [ ! -s "sirometv_updated.m3u" ]; then
          echo "❌ ERROR: Updated playlist is missing or empty!"
          exit 1
        fi
        
        # লাইন সংখ্যা চেক করা
        OLD_LINES=$(wc -l < main_playlist.m3u)
        NEW_LINES=$(wc -l < sirometv_updated.m3u)
        echo "📊 Line count - Old: $OLD_LINES, New: $NEW_LINES, Difference: $((NEW_LINES - OLD_LINES))"
        
        mv sirometv_updated.m3u sirometvsports.m3u
        
        # আপডেট করা Live Event গ্রুপ দেখান
        echo "🔍 Updated Live Event group channels (first 5):"
        grep -A 1 'group-title="Live Event"' sirometvsports.m3u | head -10 || echo "No Live Event channels found"
        
    - name: Commit and push changes
      run: |
        echo "💾 Saving changes to repository..."
        
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add sirometvsports.m3u
        
        if git diff --staged --quiet; then
          echo "✅ No changes detected - playlist is up to date"
        else
          # বাংলাদেশের সময় নেওয়া (BD Time format)
          BANGLADESH_TIME=$(TZ='Asia/Dhaka' date +'%Y-%m-%d %I:%M:%S%p BD Time')
          
          # Live Event চ্যানেল সংখ্যা গণনা
          CHANNEL_COUNT=$(grep -c 'group-title="Live Event"' sirometvsports.m3u || echo "0")
          
          # Check if manual or scheduled run
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            git commit -m "🔄 Manual Update: Live Event Group ($CHANNEL_COUNT channels) - $BANGLADESH_TIME"
          else
            git commit -m "🔄 Scheduled Update: Live Event Group ($CHANNEL_COUNT channels) - $BANGLADESH_TIME"
          fi
          git push
          echo "🎉 Successfully updated Live Event group with $CHANNEL_COUNT channels!"
        fi
        
    - name: Cleanup
      run: |
        rm -f main_playlist.m3u source_playlist.m3u
        echo "✅ Cleanup completed!"
